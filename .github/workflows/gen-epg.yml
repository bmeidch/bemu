name: Generate EPG every day
on:
  schedule:
    - cron: 0 */6 * * *
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6
      - name: config
        run: |
          git pull
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"
      - name: generate
        run: |
          cd epg
          pwd       
          python3 -m pip install lxml
          python scripts/index.py guideoe.xml scripts/main/mainoe.txt "action@github.com" "GitHub Action"
          python scripts/index.py guiamer.xml scripts/main/guiamer.txt "action@github.com" "GitHub Action"
          python scripts/index.py guiyuro.xml scripts/main/guiyuro.txt "action@github.com" "GitHub Action"
          python scripts/index.py guispo.xml scripts/main/guispo.txt "action@github.com" "GitHub Action"
          python scripts/index.py local.xml scripts/local/local.txt "action@github.com" "GitHub Action"
          python scripts/index.py nze.xml scripts/nze/nze.txt "action@github.com" "GitHub Action"

      - name: Compress XML
        working-directory: epg
        run: |
          gzip -9 -f guideoe.xml &
          gzip -9 -f guiamer.xml &
          gzip -9 -f guiyuro.xml &
          gzip -9 -f guispo.xml &
          gzip -9 -f local.xml &
          gzip -9 -f nze.xml &
          wait

      - name: Commit Changes
        run: |
          git add -A
          git commit -m "SIP Auto Update EPG"
          git push
